# 🤖 웹 트랙 W — 「얼마나 큰 웹인가」 판정 대기

> **콜드 세션 시작점.** 사람이 *"뭐 하고 있었어? 이어서 하자"* 라고 물으면 **이 문서로 답한다.**
> 상태 정본은 [`ROADMAP.md`](ROADMAP.md) Now의 「▶ 재개 지점」 — 충돌하면 ROADMAP이 이긴다.

**한 줄**: 웹사이트를 **만드느냐**가 아니라 **얼마나 크게 만드느냐**를 사람이 정하는 자리에서 멈춰 있다. 코드는 한 줄도 시작하지 않았다.

---

## 0. 세션 첫 행동 — 이것부터 하고 말을 건다

```bash
open -a "Google Chrome" ~/devetym/docs/handoff/26-08-21-web-scale-decision-brief.html
```

**[`docs/handoff/26-08-21-web-scale-decision-brief.html`](docs/handoff/26-08-21-web-scale-decision-brief.html)** = 사람에게 보여줄 **결정 브리핑**(자기완결·비전문가용). 2026-08-21에 사람이 *"이해가 잘 된다"* 고 확인한 형태다.
탭은 **하나만** 띄운다. 여러 개 띄우면 사람이 헷갈려 한다(2026-08-21 실측된 피드백).

같은 내용의 웹 사본(갱신 시 같은 URL 유지): <https://claude.ai/code/artifact/c465f536-6138-4413-95b1-6043f96f2a1c>

---

## 1. 사람이 답해야 하는 것 — 세 가지 크기 중 하나

| | 작게 | 중간 | 크게 |
|---|---|---|---|
| 형태 | 소개 페이지 3~5장 | **650개 전부 웹페이지**(읽기 전용) | 650장 + **웹에서도 AI 답변** |
| 커뮤니티 글 발사 | 즉시 | 1~2주 뒤 | 3~4주 뒤 |
| 구글 검색 자산 | 없음 | 650장 | 650장 |
| 지출이 샐 위험 | 없음 | 없음 | 있음(상한으로 막음) |
| 나중에 키우기 | 사실상 다시 만듦 | **그대로 「크게」로 승급** | — |

**불확실한 것은 딱 하나**: 한국 개발자가 실제로 한국어로 어원을 검색하는가. 표제어 650은 전부 영어, 한국어 이름 1,097개는 앱 안에만 있다. **한 번도 확인한 적 없다.** 만들고 확인하면 답까지 두 달, 미리 확인하면 반나절(검색광고 도구·표본 20~30개).

**추천(이미 사람에게 전달됨)**: 반나절 실측 먼저 → 긍정이면 「중간」 착수, 부정이면 「작게」로 커뮤니티만 열고 웹 투자 중단.

---

## 2. 판정이 나오면 — 분기별 다음 작업

| 사람의 답 | 이 세션이 하는 일 |
|---|---|
| **「반나절 실측부터」** | 표본 20~30개 한국어 어원 질의 선정 → 구글 Keyword Planner·네이버 검색광고 키워드도구 조회 → 결과표를 사람에게. **계정 로그인은 사람 손이 필요**하므로 `! <명령>` 안내 또는 사람 조작 요청 |
| **「중간」** | W0(도메인 `devetym.com` 구매·DNS·스캐폴드·토큰 추출) → W1b(650 정적 + 검색 + 사이트맵). **W1a 프록시 하드닝은 불필요**(웹에 AI 없음) |
| **「크게」** | W0 → **W1a 프록시 하드닝 먼저**(CORS allowlist·Turnstile·표면 분리 캡) → W1b. ⚠️ **W1a가 W1b보다 반드시 먼저다** — 방어 없이 열면 웹 폭주가 앱 사용자를 429로 막는다 |
| **「작게」** | 소개 페이지 3~5장 → 씨딩 3건 발사(복붙본 = `~/Downloads/devetym-release/`, 착지 링크만 교체) |

어느 분기든 **착수 전에 ADR-0009·0010·0011 비준을 사람에게 받는다**(현재 셋 다 `Proposed`). 「중간」이면 0010·0011은 비준이 아니라 **폐기 후보**가 된다.

---

## 3. 하지 말 것

- **설계를 다시 하지 않는다.** 설계 정본은 [`docs/design/web-transition-design.md`](docs/design/web-transition-design.md)에 이미 있다. 사람이 뒤집기로 하면 그때 대안 설계를 판다.
- **과정 언어로 설명하지 않는다.** 「감사 0/26」·「L0-1」·「ADR Proposed」 같은 진행 상태 어휘로 사람에게 상황을 설명하면 **더 헷갈려 한다**(2026-08-21 피드백). 사람에게는 **결정과 트레이드오프**로 말하고, 과정 어휘는 이 문서와 원장 안에만 둔다.
- **코드·스캐폴드 착수 금지** — 위 §2의 판정이 나오기 전까지. `web/` 디렉토리는 아직 존재하지 않는다.
- **도메인 구매 금지**(사람 지시 대기). 구매하더라도 **`Constants.kt`의 방침 URL은 마지막에 바꾼다** — ① 구매 → ② `devetym.com`에 정책 페이지 실게시·응답 확인 → ③ 상수 교체 → ④ 스토어 라벨 갱신. 먼저 바꾸면 **게시된 앱이 죽은 URL을 가리킨다.**

---

## 4. 실측 숫자 (2026-08-21 재확인 — 인용해도 되는 값)

앱 자신의 `normalizeKeyword`(= `trim().lowercase()`, `shared/.../data/AppJson.kt:38`)로 재집계했다.

| 항목 | 값 |
|---|---|
| 엔트리 / 고유 keyword | 650 / 650 (충돌 0) |
| 한글 keyword | **0** — 표제어는 전부 영어 |
| 고유 별칭 키 / 그중 한글 | 1,316 / **1,097** |
| 교차 충돌 | 3 (집계·분기·샤딩) |
| 본문 중앙값 / 300자 미만 | 303.5자 / 317개(48.8%) |
| `DAILY_LIMIT` / `GLOBAL_DAILY_LIMIT` | 10 / 200 (`~/devetym-proxy/src/index.js:33-34`) |
| ADR-0009·0010·0011 | 셋 다 `Proposed` |

⚠️ **정규화 정의가 이 숫자를 바꾼다.** 구분자까지 지우는 정규화로 세면 별칭 **1,286 / 충돌 4**(`b트리` 추가)가 나온다. 웹이 `normalizeTermKey`를 vendoring할 때 정의가 어긋나면 페이지 수와 충돌 수가 조용히 달라진다.

---

## 5. 포인터

| 무엇 | 어디 |
|---|---|
| 상태 정본 | [`ROADMAP.md`](ROADMAP.md) Now 「▶ 재개 지점」 |
| 설계 정본 | [`docs/design/web-transition-design.md`](docs/design/web-transition-design.md) |
| 결정 감사 원장(0/26) | 브랜치 `docs/web-track-autonomy-prep` · `docs/handoff/26-08-05-web-design-confirmation-ledger.md` |
| 감사 진행 화면(과정 언어·AI용) | <https://claude.ai/code/artifact/5b2dc6fe-d328-4880-b902-c8c7225b27b6> |
| 씨딩 복붙본 | `~/Downloads/devetym-release/` — **폐기 금지**, 착지 링크만 교체해 재사용 |
| iOS 트랙 상태 | [`🤖-26-08-13-ios-review-submission-handoff.md`](🤖-26-08-13-ios-review-submission-handoff.md) — **역할 종료됨**(이력 보존용). 웹과 섞지 말 것 |

**살아 있는 다른 트랙 1건**: `~/Downloads/devetym-release` 정돈(미착수). 웹과 독립이며 ROADMAP 재개 지점 1번.
