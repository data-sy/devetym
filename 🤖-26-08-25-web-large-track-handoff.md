# 🤖 웹 트랙 W — 「크게」 착수 (판정 완료)

> **콜드 세션 시작점.** 사람이 *"뭐 하고 있었어? 이어서 하자"* 라고 물으면 **이 문서로 답한다.**
> 상태 정본은 [`ROADMAP.md`](ROADMAP.md) Now의 「▶ 재개 지점」 — 충돌하면 ROADMAP이 이긴다.
> 선행 문서 [`🤖-26-08-21-web-scale-handoff.md`](🤖-26-08-21-web-scale-handoff.md)는 **역할 종료**(판정 대기 상태를 다루던 문서).

**한 줄**: 규모 판정이 끝났고(**「크게」** — 650장 정적 + 웹 AI, 3~4주) ADR 3건도 비준됐다. **도메인 구매를 기다리지 않고 W0a부터 착수한다**(2026-08-25 사람 결정 — 아래 §0). 코드는 아직 한 줄도 없다(`web/` 디렉토리 없음).

---

## 0. 새 세션이 *"이어서 하자"* 를 들었을 때 — 바로 이것을 한다

**다음 한 걸음 = W0a 착수**(아래 §2). 사람에게 되물을 것 없다 — 게이트는 다 통과했거나 우회 결정이 나 있다.

```bash
# 착수 지점 확인만 하고 바로 시작
ls ~/devetym/web 2>/dev/null || echo "web/ 없음 = W0a 미착수 상태"
```

첫 산출물: `web/` Astro 스캐폴드 + `SITE_URL` 단일 지점 + 디자인 토큰 CSS. **도메인 없이 전부 된다.**

---

## 1. 결정 (2026-08-25 · 사람)

| | 내용 |
|---|---|
| **판정** | **「크게」** — 650장 전부 웹페이지 + 웹 AI 폴백 |
| **ADR** | 0009·0010·0011 **비준 완료**(2026-08-25) · Q5 = W1a 포함 · Q4 = 앱 유도 전면 |
| **도메인** | **기다리지 않는다**(2026-08-25) — `SITE_URL` 단일 지점 + 미리보기 서브도메인으로 W0a 선행. 가짜 호스트명 금지 |
| **근거** | 수요 실측이 아니라 **구조**: *"어차피 웹에서도 AI가 돌게 하는 게 궁극적으로 원하는 형태"*. 「중간」(읽기 전용 650장)은 지나갈 중간역일 뿐 승급 비용만 든다 |
| **부수 결과 ①** | **검색 수요 선실측 스파이크 취소.** 그 반나절 런북은 「중간 vs 작게」를 가르는 장치였고 결정이 그 위를 지나갔다 → `docs/handoff/archive/26-08-24-search-demand-spike-runbook.html`에 보존(되살릴 일 없으면 열지 말 것) |
| **부수 결과 ②** | ADR-0010(웹 AI 남용 방지)·ADR-0011(프롬프트 소유권 이전)이 **폐기 후보가 아니라 비준 대상**으로 확정 |
| **눈 뜨고 수용한 위험** | 한국어 어원 검색 수요는 **여전히 미검증**이다. 사전 측정으로 닫지 않고 **W3 실측(색인률·유입)으로 사후 확인**한다. 하방 완충: 씨딩 착지면과 웹 AI 자체는 검색 수요와 무관하게 성립 |

**이 결정을 다시 논쟁하지 않는다.** 뒤집을 새 사실이 나오면 그때 ROADMAP에 새 결정으로 적는다.

---

## 2. 다음 순서 — 위에서 아래로

### ✅ 게이트 A · ADR 3건 비준 — 완료 (2026-08-25 사람)
셋 다 `Proposed` → **`Accepted`**.

| ADR | 무엇을 못 박았나 |
|---|---|
| [ADR-0009](docs/adr/0009-web-framework-rendering.md) | Astro + React 아일랜드 · 650 SSG · Cloudflare. **Kotlin/Wasm·Next.js 기각 재론 금지** |
| [ADR-0010](docs/adr/0010-web-abuse-prevention.md) | 표면 분리 + Turnstile + 3층 한도. **웹 사용자 대면 3건/일 = AI 신규 생성 3건**(열람은 무제한) |
| [ADR-0011](docs/adr/0011-prompt-ownership-transfer.md) | 시스템 프롬프트 `commonMain` → Worker. **착수 시점 = W1a**(Q5 종결 — 정본이 2벌이 되는 창을 만들지 않는다) |

**함께 정해진 것 (Q4 방향)**: 한도 도달 화면은 **앱 유도를 전면에** — 주 버튼 = 스토어 링크("무제한은 앱에서"), 사전 열람 650장은 계속 무제한임을 보조로 병기. 유일하게 측정 가능한 전환 경로다. **문구 최종안은 W1b에서 2~3안을 만들어 사람이 고른다.**

### ⏸ 게이트 B · `devetym.com` 구매 — **더 이상 차단 요인이 아니다 (2026-08-25 우회 결정)**
사람 결정: *"도메인은 샀다 치고 우선 진행"*. **단, 존재하지 않는 호스트명(`www.devetym.com` 등)을 코드에 박지 않는다** — 그러면 이 트랙의 완료 오라클(*배포된 실 URL 650개 전수 200 응답*)이 통째로 실행 불가가 되고, 이 프로젝트가 마일스톤마다 겪은 *"빌드는 되는데 실기동은 깨진다"*의 웹 버전을 자초한다.

**대신 이렇게 한다**:
- 도메인 참조는 **`SITE_URL` 단일 지점**에서만 읽는다 — canonical·사이트맵·OG·구조화 데이터·내부 절대링크 전부. 하드코딩 0건.
- 기본값 = **Cloudflare 미리보기 서브도메인**(`*.pages.dev` / `*.workers.dev` — 공짜이고 **실존**한다). 그래서 650장 200 응답 오라클을 도메인 없이도 실제로 돌릴 수 있다.
- 구매하면 `SITE_URL` 한 줄 교체 + DNS. 그게 전부다.

**순서 게이트는 그대로 유효**(구매했을 때): ① 구매 → ② `devetym.com`에 정책 페이지 실게시·200 응답 확인 → ③ `Constants.kt`의 `privacyPolicyUrl` 교체 → ④ App Store 라벨 갱신. 역순이면 **게시된 iOS 앱이 죽은 URL을 가리킨다**(현행 라이브 = `https://data-sy.github.io/devetym/privacy-policy`).

### W0a · 기반 〔지금 착수 가능 · 도메인 불필요 · Claude 자율〕
Astro 스캐폴드(`~/devetym/web/`) · **`SITE_URL` 단일 지점 설계** · **디자인 토큰 추출**(`ui/theme/AppColors.kt`·`AppTypography.kt`·`AppDimens.kt` → CSS 변수) · 폰트 인라인 · 미리보기 서브도메인 배포.
**착수 시 실측할 것**: Astro + Cloudflare Workers 어댑터 성숙도(설계서 §8-1 — 미검증으로 남겨둔 항목).
완료 오라클: **빈 사이트가 미리보기 URL에 실제 배포되어 200 응답**.

### W0b · 도메인 결선 〔구매 후 · 몇 분〕
DNS 연결 · `SITE_URL` 교체 · **Search Console 소유권 확인**. 도메인 없이 못 하는 것은 **이 셋뿐**이고, 색인률(K1) 측정이 여기서 시작된다.

### W1a · 프록시 하드닝 〔⚠️ W1b보다 반드시 먼저〕
`~/devetym-proxy` 수정 — CORS allowlist · Turnstile · **표면 분리 캡** · 워크스페이스 분리 · **프롬프트 소유권 이전(ADR-0011)** · **usage 로그에 표면 태그**(없으면 F5 = 앱 사용자 429/402 퇴행을 관측할 수단이 없다).

| 층 | 기준 | 값 |
|---|---|---|
| 사용자 대면(웹) | 브라우저 쿠키/localStorage | **3건/일** — UX 한도 겸 앱 유도 전환점 |
| 남용 방지(웹) | IP (`CF-Connecting-IP`) | **15건/일** — CGNAT 감안한 백스톱 |
| 예산(웹) | 웹 전역 | **30건/일** |
| 앱(현행 유지) | 기기 / 앱 전역 | 10 / 200건/일 |

완료 오라클: **기존 iOS 앱에서 생성 정상 성공(무영향 실측)** + 브라우저에서 토큰 없이 호출 시 차단 확인.

### W1b · 웹 본체
정적 650장 + 검색 + 상세 + **AI 폴백** + 사이트맵. **한글 별칭 1,097개를 title·h1·구조화 데이터에 1급으로 올린다** — 안 하면 650장은 한국어 검색에 존재하지 않는다.
완료 오라클: **배포된 실 URL 650개 전수 200 응답** + 사이트맵 제출 + AI 생성 왕복 성공. **로컬 빌드 성공은 오라클이 아니다.**

### W2 · W3
W2 = 카테고리 허브·관련 용어·구조화 데이터·얇은 콘텐츠 대응(317개가 300자 미만) → 색인률 측정 개시. W3 = 8주 실측 리뷰(여기서 **검색 수요 가정이 사후 판정된다**).

---

## 3. 하지 말 것

- **설계를 다시 하지 않는다.** 정본 = [`docs/design/web-transition-design.md`](docs/design/web-transition-design.md). 규모 선택지(작게/중간/크게) 재론 금지 — 끝났다.
- **W1b를 W1a보다 먼저 열지 않는다.** 방어 없이 웹 AI를 열면 웹 폭주가 앱 사용자를 429로 막는다.
- **ADR·specs·architecture를 승인 없이 고치지 않는다**(거버넌스 규율). 비준된 0009~0011의 결정을 조용히 우회하지도 않는다 — 뒤집으려면 새 ADR.
- **과정 어휘로 사람에게 설명하지 않는다.** 「감사 0/26」·「L0-1」·「Proposed」 같은 말 대신 **결정과 트레이드오프**로 말한다. 참고 자료는 **한 번에 한 탭만**.
- 검색 수요 실측을 다시 제안하지 않는다 — 사람이 그 위를 지나가기로 결정했다.

---

## 4. 인용해도 되는 실측값 (2026-08-21 재확인)

| 항목 | 값 |
|---|---|
| 엔트리 / 고유 keyword | 650 / 650 (충돌 0) |
| 한글 keyword | **0** — 표제어는 전부 영어 |
| 고유 별칭 키 / 그중 한글 | 1,316 / **1,097** |
| 교차 충돌 | 3 (집계·분기·샤딩) |
| 본문 중앙값 / 300자 미만 | 303.5자 / 317개(48.8%) |
| `DAILY_LIMIT` / `GLOBAL_DAILY_LIMIT` | 10 / 200 (`~/devetym-proxy/src/index.js:33-34`) |
| Anthropic 월 예산 | $30 유지(선제 상향 없음) |

⚠️ **정규화 정의가 이 숫자를 바꾼다.** 구분자까지 지우는 정규화로 세면 별칭 1,286 / 충돌 4(`b트리`). 웹이 `normalizeTermKey`를 vendoring할 때 정의가 어긋나면 페이지 수와 충돌 수가 조용히 달라진다.

---

## 5. 포인터

| 무엇 | 어디 |
|---|---|
| 상태 정본 | [`ROADMAP.md`](ROADMAP.md) Now 「▶ 재개 지점」 |
| 설계 정본 | [`docs/design/web-transition-design.md`](docs/design/web-transition-design.md) |
| 서버 코드 | `~/devetym-proxy` (별도 repo) · 계약 = [ADR-0006](docs/adr/0006-server-cache-boundary.md) |
| 웹 코드 배치 | `~/devetym/web/` (같은 repo 서브디렉토리 — `terms.json` 참조로 수정 지점 1) |
| 투자자용 브리핑(사람에게 보여줄 한 장) | [`docs/handoff/26-08-25-web-large-investor-brief.html`](docs/handoff/26-08-25-web-large-investor-brief.html) |
| 버그·개선 | GitHub Issues ([ADR-0008](docs/adr/0008-issue-tracking.md)) |
| 씨딩 복붙본 | `~/Downloads/devetym-release/` — **폐기 금지**, 착지 링크만 교체해 재사용. 발사 시점 = 웹 완성 후 |
| 살아 있는 다른 트랙 | `~/Downloads/devetym-release` 정돈(미착수, 웹과 독립) |
